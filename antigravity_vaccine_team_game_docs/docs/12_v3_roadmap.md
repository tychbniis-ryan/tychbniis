# 12 第 3 版製作路線圖

## 版本定位

第 3 版目標是把 `docs/01_game_rules.md` 的完整遊戲規則，拆成可維護、可測試、可逐步上線的功能版本。

第 3 版不改變第 2 版已定版架構：

1. Firebase Hosting：學員端與講師端入口。
2. Firebase Realtime Database：只放公開狀態、公開題庫與公開排行榜。
3. GAS / Google Sheets：正式資料來源、規則判斷、計分、抽獎與報表。
4. Cloud Functions：免費方案暫停，不納入第 3 版必要條件。

## 第 3 版核心原則

1. 先補資料結構，再補 API，再補 UI。
2. 抽寶箱、道具、幸運獎、全對獎與票選判定都必須在 GAS 執行。
3. 學員端不得取得正確答案、未公開分數細節、抽獎亂數種子或管理設定。
4. 每次只上線一個子功能，避免活動現場一次承受多個新風險。
5. 第 2 版核心流程可獨立運作；第 3 版功能失效時，仍可退回第 2 版問答與排行榜。

## 規則來源

第 3 版以 `docs/01_game_rules.md` 為唯一規則來源。已納入的規則如下：

1. 5 個戰隊。
2. 約 200 名學員。
3. 戰隊勝利條件為戰隊加權平均分。
4. 個人答題分數依答對時間給分。
5. 每題最快答對者加 5 分。
6. 每人最多持有 3 個寶箱。
7. 幸運獎全場 1 名。
8. 全對獎 3 名，依完成時間排序。
9. 寶箱可抽到加分卡、加倍卡、翻身卡、挑戰卡、特殊道具或空寶箱。
10. 創作票選題分為隊內初選、講師選代表、匿名全體投票。

## P0：第 3 版基礎資料結構

### 問題

目前第 2 版已能完成問答、計分與排行榜，但缺少寶箱、道具、獎項與票選資料表。若直接改 UI，資料會無法追蹤，也不利於活動後查核。

### 解法

新增或規劃下列 Google Sheets 工作表：

| 工作表 | 用途 | 是否含敏感資料 |
|---|---|---|
| 寶箱紀錄 | 記錄寶箱取得、開啟、失效與來源 | 否 |
| 道具紀錄 | 記錄抽到的道具、使用時機與結果 | 否 |
| 獎項紀錄 | 記錄幸運獎與全對獎得主 | 否 |
| 創作投稿 | 記錄創作票選題投稿內容 | 可能含自由文字 |
| 創作投票 | 記錄隊內初選與全體投票 | 否 |
| 規則設定 | 保存寶箱機率、持有上限、投票秒數 | 否 |

### 驗收標準

1. `setupGameSheets` 可建立第 3 版工作表。
2. `resetGameData` 可清除第 3 版活動紀錄，但保留規則設定。
3. 所有第 3 版工作表欄位寫入 `gas/README.md`。
4. 不記錄完整姓名、身分證字號、帳密或 Token。

## P1：寶箱取得與持有限制

### 問題

規則要求多種寶箱取得條件，且每人最多持有 3 個。這必須由後端統一判斷，避免學員端自行計算造成作弊或不同步。

### 解法

GAS 在關題計分後判斷寶箱取得：

1. 每題答對有 30% 機率取得 1 個寶箱。
2. 正確回答 3 題取得 1 個寶箱。
3. 正確回答 5 題取得 1 個寶箱。
4. 正確回答 10 題取得 2 個寶箱。
5. 連續答對 3 題取得 1 個寶箱。
6. 連續答對 5 題取得 2 個寶箱。
7. 使用道具 3 次取得 1 個寶箱。
8. 使用道具 5 次取得 1 個寶箱。

持有限制：

1. 每人最多 3 個未開啟寶箱。
2. 超過 3 個時，系統自動丟棄最早取得且尚未開啟的寶箱。
3. 活動結束時，未使用寶箱標記為失效。

### API 規劃

| action | 使用者 | 說明 |
|---|---|---|
| getPlayerInventory | 學員 | 讀取自己的未開啟寶箱與可用道具 |
| openTreasureBox | 學員 | 開啟自己的寶箱 |
| expireUnusedBoxes | 講師 | 活動結束後讓未開啟寶箱失效 |

### 驗收標準

1. 同一題重複關題不會重複發寶箱。
2. 同一學員重新整理頁面後，寶箱狀態不重複產生。
3. 未開啟寶箱最多保留 3 個。

## P2：道具效果與戰隊加成

### 問題

第 2 版排行榜主要依答題分數排序。第 3 版規則要求戰隊加權平均分與道具加成，必須明確拆分「個人答題分」、「戰隊道具加成」與「最終排名分」。

### 解法

戰隊最終分數採下列公式：

```text
戰隊加權平均分 = 戰隊總得分 / 有效參與人數 + 戰隊任務加成
```

其中：

1. 戰隊總得分：所有有效學員的個人答題分加總。
2. 有效參與人數：至少完成 1 題作答的學員。
3. 戰隊任務加成：加分卡、翻身卡、挑戰卡等道具產生的隊伍加成。

道具規則：

| 道具 | 後端判定方式 |
|---|---|
| 小加分卡 | 題後使用，戰隊 +1，每題限用 1 張 |
| 中加分卡 | 題後使用，戰隊 +3，每題限用 1 張 |
| 大加分卡 | 題後使用，戰隊 +5，每題限用 1 張 |
| 超級加分卡 | 題後使用，戰隊 +10，每題限用 1 張 |
| 加倍卡 | 題前使用，下一題答對時個人分數 x2，額外加成上限 20 分 |
| 翻身卡 | 題後使用，本隊最後一名 +30，否則 +5，每隊最多觸發 2 次 |
| 挑戰卡 | 題前指定一隊，下一題本隊答對率較高 +10，否則 +3 |
| 特殊道具 | 取得即成為幸運獎，且全場只產生 1 名 |

### API 規劃

| action | 使用者 | 說明 |
|---|---|---|
| useItem | 學員 | 使用指定道具 |
| getTeamBonusLedger | 講師 | 讀取戰隊道具加成明細 |
| recalculateV3Scoreboard | 講師 | 重新計算第 3 版排行榜 |

### 驗收標準

1. 個人答題分與戰隊道具加成分開保存。
2. 加倍卡不提前揭露下一題答案。
3. 挑戰卡不扣對方分數。
4. 翻身卡每隊最多觸發 2 次。
5. 幸運獎產生後，特殊道具不再進入抽獎結果。

## P3：全對獎與幸運獎

### 問題

全對獎與幸運獎都會影響頒獎結果。若只靠現場人工判斷，活動後難以追溯。

### 解法

由 GAS 在活動結束或講師手動結算時產生獎項紀錄：

1. 全對獎：
   - 全部正式題目皆答對。
   - 依完成最後一題的送出時間排序。
   - 取前 3 名。
2. 幸運獎：
   - 第一位抽中特殊道具者取得。
   - 幸運獎產生後，特殊道具獎池關閉。
   - 若完成總題數 70% 後仍未產生，特殊道具機率由 3% 提高為 10%，直到產生為止。

### API 規劃

| action | 使用者 | 說明 |
|---|---|---|
| finalizeAwards | 講師 | 結算幸運獎與全對獎 |
| getAwardList | 講師 | 讀取頒獎名單 |

### 驗收標準

1. 全對獎最多 3 名。
2. 幸運獎最多 1 名。
3. 重新結算不會重複產生不同得主，除非講師先執行初始化。

## P4：創作票選題

### 問題

創作題包含自由文字、隊內初選、講師選代表與匿名全體投票。這是第 3 版最高風險功能，因為自由文字可能出現不適當內容，也可能暴露暱稱與戰隊。

### 解法

採四段式流程：

1. 學員提交 1 則答案。
2. 系統依戰隊分流，同隊只看同隊投稿池。
3. 隊內初選 60 秒，每隊取前 3 名候選。
4. 講師每隊選 1 則代表作品，再匿名為作品 A 至 E，開放全體投票 60 秒。

限制：

1. 前端隱藏戰隊與暱稱。
2. 後端限制不可投自己隊。
3. 講師端必須先審核代表作品後，才可開放全體投票。
4. 自由文字不得寫入公開 Firebase 路徑，除非已匿名且經講師確認。

### API 規劃

| action | 使用者 | 說明 |
|---|---|---|
| submitCreativeAnswer | 學員 | 提交創作答案 |
| getTeamCreativePool | 學員 | 讀取同隊投稿池 |
| voteTeamCreative | 學員 | 隊內初選投票 |
| selectCreativeFinalists | 講師 | 講師選代表作品 |
| getCreativeFinalists | 學員 | 讀取匿名作品 A 至 E |
| voteCreativeFinal | 學員 | 全體投票 |
| getCreativeVoteResult | 講師 | 讀取票選結果 |

### 驗收標準

1. 每位學員只能提交 1 則答案。
2. 隊內初選只能投同隊投稿。
3. 全體投票不可投自己隊。
4. 全體投票頁不顯示戰隊、暱稱或玩家 ID。
5. 講師未審核前，作品不公開給全體。

## 第 3 版建議開發順序

1. `0.3.0`：第 3 版規格、資料表與文件定義。
2. `0.3.1`：寶箱資料表、寶箱取得與持有限制。
3. `0.3.2`：開寶箱與道具庫讀取。
4. `0.3.3`：加分卡、加倍卡、翻身卡、挑戰卡。
5. `0.3.4`：幸運獎與全對獎結算。
6. `0.3.5`：戰隊加權平均分排行榜。
7. `0.3.5-ui`：補做學員端寶箱與道具 UI。
8. `0.3.6`：創作題投稿與隊內初選。
9. `0.3.7`：講師審核代表作品與匿名全體投票。
10. `0.3.8`：賽後報表匯出。

## 0.3.1 完成範圍

Status：`0.3.1` 已完成本機製作，尚未部署雲端。
Root Cause：第 3 版需先具備可追蹤的寶箱資料與持有限制，後續才適合做開箱與道具 UI。
Suggested Fix：下一版進入 `0.3.2`，實作開箱與道具庫讀取，不先處理道具效果。

已完成：

1. `setupGameSheets` 建立第 3 版基礎工作表：
   - `寶箱紀錄`
   - `道具紀錄`
   - `獎項紀錄`
   - `創作投稿`
   - `創作投票`
   - `規則設定`
2. `規則設定` 預設寫入：
   - `maxBoxesPerPlayer`
   - `boxDropRateOnCorrect`
   - `teamVoteSeconds`
   - `finalVoteSeconds`
   - `luckyPrizeLimit`
   - `perfectPrizeLimit`
3. `closeAndScoreQuestion` 對新計分且答對的作答紀錄發寶箱。
4. 寶箱取得條件已完成：
   - 每題答對 30% 機率取得 1 個寶箱。
   - 累積答對 3 題取得 1 個寶箱。
   - 累積答對 5 題取得 1 個寶箱。
   - 累積答對 10 題取得 2 個寶箱。
   - 連續答對 3 題取得 1 個寶箱。
   - 連續答對 5 題取得 2 個寶箱。
5. 每位學員最多保留 3 個未開啟寶箱；超過時自動將最早未開啟寶箱標記為 `discarded`。
6. 新增 `getPlayerInventory` API，供後續 UI 讀取寶箱與道具狀態。

未完成：

1. 開箱與道具庫讀取已於 `0.3.2` 完成。
2. 尚未實作道具效果。
3. 尚未更新學員端 UI。
4. 尚未部署 GAS Web App。

## 0.3.2 完成範圍

Status：`0.3.2` 已完成本機製作，尚未部署雲端。
Root Cause：`0.3.1` 已能產生寶箱，但缺少開箱與道具庫資料，學員端後續無法顯示可用道具。
Suggested Fix：下一版進入 `0.3.3`，實作加分卡、加倍卡、翻身卡與挑戰卡效果；在那之前道具只記錄、不生效。

已完成：

1. 新增 `openTreasureBox` API。
2. 僅允許玩家開啟自己的 `unopened` 寶箱。
3. 開箱後更新 `寶箱紀錄`：
   - `status=opened`
   - `openedAt`
   - `itemType`
4. 非空寶箱會新增 `道具紀錄`：
   - `status=available`
   - `sourceBoxId`
   - `itemType`
5. `getPlayerInventory` 回傳：
   - 寶箱 `itemType`
   - 寶箱 `itemLabel`
   - 道具 `sourceBoxId`
   - 道具 `targetQuestionId`
   - 道具 `targetTeamId`
   - 道具 `effectScore`
6. `規則設定` 新增寶箱獎項機率預設值。

未完成：

1. 道具效果已於 `0.3.3` 完成。
2. 尚未處理幸運獎產生後關閉特殊道具獎池。
3. 尚未更新學員端 UI。
4. 尚未部署 GAS Web App。

## 0.3.3 完成範圍

Status：`0.3.3` 已完成本機製作，尚未部署雲端。
Root Cause：`0.3.2` 已能開箱與建立道具，但道具尚未影響個人分數或戰隊加成。
Suggested Fix：下一版進入 `0.3.4`，實作幸運獎與全對獎結算；特殊道具的幸運獎效果仍保留給 `0.3.4`。

已完成：

1. 新增 `useItem` API。
2. 加分卡效果：
   - 小加分卡：戰隊 +1。
   - 中加分卡：戰隊 +3。
   - 大加分卡：戰隊 +5。
   - 超級加分卡：戰隊 +10。
   - 每隊同一題同類加分卡限用 1 張。
3. 加倍卡效果：
   - 題前指定目標題。
   - 關題計分時若答對，個人分數額外加成。
   - 額外加成上限 20 分。
4. 翻身卡效果：
   - 題後使用。
   - 使用當下若本隊最後一名，戰隊 +30。
   - 否則戰隊 +5。
   - 每隊最多觸發 2 次。
5. 挑戰卡效果：
   - 題前指定目標題與對手戰隊。
   - 目標題關題後比較本隊與對手答對率。
   - 本隊答對率較高則戰隊 +10，否則 +3。
   - 不扣對方分數。
6. 新增 `getTeamBonusLedger` API。
7. 新增 `recalculateV3Scoreboard` API。
8. `排行榜` 新增：
   - `teamBonusScore`
   - `finalScore`
   - `weightedAverageScore`

未完成：

1. 特殊道具幸運獎已於 `0.3.4` 完成。
2. 全對獎已於 `0.3.4` 完成。
3. 尚未更新學員端 UI。
4. 尚未部署 GAS Web App。

## 0.3.4 完成範圍

Status：`0.3.4` 已完成本機製作，尚未部署雲端。
Root Cause：`0.3.3` 已完成基本道具效果，但缺少活動結束後的幸運獎與全對獎結算。
Suggested Fix：下一版進入 `0.3.5`，處理學員端寶箱與道具 UI，讓學員可操作已完成的 GAS API。

已完成：

1. 新增 `finalizeAwards` API。
2. 新增 `getAwardList` API。
3. 幸運獎：
   - 第一位抽中特殊道具者取得幸運獎。
   - 特殊道具出現後，後續開箱不再抽出特殊道具。
   - 正式題目開放進度達 70% 且尚未出現特殊道具時，特殊道具機率由 3% 提高為 10%。
4. 全對獎：
   - 全部正式題目皆答對者才具備資格。
   - 依完成最後一題的送出時間排序。
   - 預設取前 3 名。
5. `道具紀錄` 新增 `createdAt` 欄位，供幸運獎排序使用。
6. `獎項紀錄` 新增 `nickname`、`score`、`completedAt`、`sourceItemId` 欄位。

未完成：

1. 尚未更新學員端寶箱與道具 UI。
2. 尚未更新講師端獎項結算 UI。
3. 尚未部署 GAS Web App。

## 0.3.5 完成範圍

Status：`0.3.5` 已完成本機製作，尚未部署雲端。
Root Cause：第 3 版勝利條件使用戰隊加權平均分，但 `0.3.4` 前排行榜仍未明確區分報到人數與有效參與人數。
Suggested Fix：下一版進入 `0.3.6`，依路線圖處理創作題投稿與隊內初選。

已完成：

1. `排行榜` 新增 `effectivePlayerCount` 欄位。
2. `playerCount` 保留報到人數。
3. `effectivePlayerCount` 代表至少完成 1 題已計分作答的有效參與人數。
4. `averageScore = totalScore / effectivePlayerCount`。
5. `weightedAverageScore = averageScore + teamBonusScore`。
6. 啟用中的戰隊即使尚無有效參與者，也會保留在排行榜並顯示 0 分。
7. 學員端排行榜顯示排名分、有效人數與道具加成。
8. 講師端排行榜顯示排名分、有效人數、答題總分、答題平均、道具加成與最終總分。

未完成：

1. 學員端寶箱與道具 UI 已於 `0.3.5-ui` 補作完成。
2. 尚未更新講師端獎項結算 UI。
3. 尚未部署 GAS Web App 或 Firebase Hosting。

## 0.3.5-ui 補作範圍

Status：`0.3.5-ui` 已完成本機製作，尚未部署雲端。
Root Cause：前次 `0.3.5` 依路線圖完成排行榜，但使用者原本期待同版完成學員端寶箱與道具 UI。
Suggested Fix：先補作學員端 UI，完成 commit 後再進入 `0.3.6`。

已完成：

1. 學員端新增「寶箱與道具」區塊。
2. 可讀取自己的寶箱與道具。
3. 可開啟自己的未開啟寶箱。
4. 可使用已支援道具。
5. 道具使用欄位包含目標題目與挑戰戰隊。
6. 特殊道具只顯示幸運獎狀態，不在前端套用效果。
7. 前端不自行計算抽獎、道具效果或分數。

未完成：

1. 尚未更新講師端獎項結算 UI。
2. 尚未部署 GAS Web App 或 Firebase Hosting。

## 0.3.6 完成範圍

Status：`0.3.6` 已完成本機製作，尚未部署雲端。
Root Cause：第 3 版創作題需要先建立學員投稿與隊內初選流程，後續講師才能審核代表作品。
Suggested Fix：下一版進入 `0.3.7`，處理講師審核代表作品與匿名全體投票。

已完成：

1. 新增 `submitCreativeAnswer` API。
2. 新增 `getTeamCreativePool` API。
3. 新增 `voteTeamCreative` API。
4. 每位學員每場只能提交 1 則創作答案。
5. 同隊投稿池不回傳投稿者暱稱與 playerId。
6. 隊內初選只能投同隊投稿。
7. 每位學員每場隊內初選只能投 1 票。
8. 學員端新增創作題投稿、同隊投稿池與隊內初選投票介面。
9. `創作投稿` 新增 `selectedByInstructor` 欄位，預留給講師審核。

未完成：

1. 尚未實作講師審核代表作品。
2. 尚未實作匿名全體投票。
3. 尚未部署 GAS Web App 或 Firebase Hosting。

## 0.3.7 完成範圍

Status：`0.3.7` 已完成本機製作，尚未部署雲端。
Root Cause：`0.3.6` 已完成投稿與隊內初選，但缺少講師選代表作品與匿名全體投票流程。
Suggested Fix：下一版進入 `0.3.8`，處理賽後報表匯出。

已完成：

1. 新增 `getTeamCreativeCandidates` API。
2. 新增 `selectCreativeFinalists` API。
3. 新增 `getCreativeFinalists` API。
4. 新增 `voteCreativeFinal` API。
5. 新增 `getCreativeVoteResult` API。
6. 講師端可讀取各隊隊內候選，並每隊選 1 則代表作品。
7. 系統以 A 至 E 匿名代號顯示決選作品。
8. 學員端可讀取匿名決選作品並投票。
9. 學員端不顯示來源戰隊、暱稱或 playerId。
10. 後端限制不可投自己戰隊作品。
11. 後端限制每位學員匿名全體投票只能投 1 票。
12. 講師端可讀取匿名全體投票結果。

未完成：

1. 賽後報表匯出已於 `0.3.8` 完成。
2. 尚未部署 GAS Web App 或 Firebase Hosting。

## 0.3.8 完成範圍

Status：`0.3.8` 已完成本機製作，尚未部署雲端。
Root Cause：第 3 版功能已完成主要活動流程，但缺少活動後可交接與查核的統整報表。
Suggested Fix：下一步應進行本機總檢查與雲端部署前確認，再由使用者決定是否部署 GAS 與 Hosting。

已完成：

1. 新增 `exportGameReport` API。
2. 講師端新增「匯出賽後報表」按鈕。
3. 匯出前會重新計算排行榜。
4. 匯出前會重新結算幸運獎與全對獎。
5. 報表會建立新的 Google 試算表。
6. 報表包含：
   - 報表摘要。
   - 戰隊排行榜。
   - 個人排行榜。
   - 作答紀錄。
   - 寶箱紀錄。
   - 道具紀錄。
   - 獎項紀錄。
   - 創作投稿。
   - 創作投票。
   - 創作決選結果。
7. 報表不輸出管理密碼、Token、服務帳戶資訊。
8. 創作投票報表不輸出 `voterPlayerId`。

未完成：

1. 尚未部署 GAS Web App 或 Firebase Hosting。
2. 尚未進行雲端端到端測試。

## 0.3.9 修正範圍

Status：`0.3.9` 已完成並已部署雲端。

Root Cause：`0.3.8` 已完成第 3 版主要功能，但仍有規則與 UI 需求差異：題庫範例缺少創作題、加倍卡與挑戰卡仍要求選題、創作題區塊常駐、寶箱成就未視覺化、賽後報表顯示在講師 UI、講師控制台分區顯示方式需調整。

Suggested Fix：已完成本機檢查、GAS Web App deployment 更新與 Firebase Hosting 部署；正式活動前需由講師端初始化測試資料。

完成項目：

1. 題庫範例改為 11 題，其中 1 題為創作題。
2. 加分卡立即套用，不需選題。
3. 加倍卡自動套用下一題，答對時分數直接乘以 2。
4. 挑戰卡只選戰隊，並自動套用下一題結果。
5. 沒有下一題時，GAS 拒絕使用加倍卡與挑戰卡。
6. 學員端新增浮動寶箱與成就選單。
7. 創作題回答區只在講師開放創作題時顯示。
8. 匿名全體投票只在講師選出代表作品後顯示。
9. 講師端隱藏賽後報表 UI，但保留 API。
10. 講師端加入有效人數與報到人數說明。

## 0.3.10 修正範圍

Status：`0.3.10` 已完成並已部署雲端。

Root Cause：`0.3.9` 已完成主要規則修正，但仍有現場操作與計分呈現差異：加倍卡缺少每人一次限制，學員端仍可在報到流程中看到選隊概念，講師啟動後仍存在自由選隊切換入口，排行榜仍保留有效人數顯示。

Suggested Fix：已完成後端鎖定啟動前設定、前端啟動後報到與方塊選隊、排行榜報到人數與答對率調整，並已部署 GAS Web App 與 Firebase Hosting。

完成項目：

1. 加倍卡每位學員只能取得或使用 1 次；重複抽到改為大加分卡。
2. GAS `createGame` 啟動場次後寫入 `created` 狀態。
3. GAS `joinGame` 拒絕 `draft` 場次報到。
4. GAS `setTeamChoiceMode` 只允許 `draft` 狀態變更。
5. 學員端報到頁移除下拉式選隊。
6. 學員端自由選隊改為方塊按鈕。
7. 講師端啟動後鎖定自由選隊設定。
8. 排行榜改用報到人數計算平均分，並顯示答對率。
9. 挑戰卡答對率比較改以報到人數作分母，未作答視同錯誤。
10. 選隊 UI 增加 `.art-slot`，供未來替換美術圖。

## 0.3.11 修正範圍

Status：`0.3.11` 已完成並部署雲端。

Root Cause：`0.3.10` 已修正報到選隊與答對率基礎邏輯，但仍有現場顯示與操作流程差異：答對率需要分整體與當前題目，成就寶箱需要由學員主動領取，挑戰卡不應常駐顯示選隊欄位，講師端不需要答對率說明區。

Suggested Fix：新增成就領取 API，排行榜增加當前題目答對率，前端將挑戰卡選隊改為使用時彈出方塊按鈕，並調整顯示文字。

完成項目：

1. 「報到人數」顯示改為「戰隊人數」。
2. 排行榜分開顯示整體答對率與當前題目答對率。
3. 挑戰卡以使用後下一題的當前題目答對率結算。
4. 講師端後端設定與啟動場次完成後自動隱藏。
5. 寶箱紅點只在有未開啟寶箱時顯示。
6. 成就紅點只在有可領取成就寶箱時顯示。
7. 成就寶箱需由學員點「領取」後才建立。
8. 挑戰卡使用時才顯示方塊選隊。
9. 講師端移除答對率說明區塊。
10. 學員端題目提示改為「第 N 題」。

部署與檢查：

1. GAS 已推送並更新既有 Web App deployment 到 version 26，正式 `/exec` URL 不變。
2. Firebase Hosting 已部署學員端與講師端。
3. 線上學員端與講師端回應 `200`，並載入 `app.js?v=0.3.11`。
4. GAS `getGameState` 回應 `ok:true`。
5. GAS `claimAchievementReward` 已不再回覆「未知 action」。
6. 本次未部署 Cloud Functions、Firestore rules 或 Realtime Database rules。

## 0.3.12 修正範圍

Status：`0.3.12` 已完成並部署雲端。

Root Cause：`0.3.11` 已完成排行榜與成就領取修正，但學員端仍顯示部分內部資訊，創作題與投票尚未具備明確倒數與放棄規則，也缺少講師可控制的電腦學員測試流程。

Suggested Fix：調整學員端顯示、補上創作題與匿名投票限時規則，並新增講師端電腦學員控制 API 與 UI。

完成項目：

1. 最上方戰隊積分改用含道具加分後的排名分。
2. 寶箱與道具列表移除來源、時間與內部題目 ID。
3. 已開啟或已處理寶箱不再顯示於寶箱列表。
4. 空寶箱回傳短句提示。
5. 創作題作答時間固定為 180 秒。
6. 創作題支援主動放棄回答。
7. 全員提交或放棄，或 180 秒到，進入 30 秒隊內初選投票。
8. 匿名全體投票開放 30 秒，逾時不可投票。
9. 講師端新增「加入電腦學員」與「電腦作答目前題目」。
10. GAS 新增 `addComputerPlayers` 與 `submitComputerAnswers`。

部署與檢查：

1. GAS 已推送並更新既有 Web App deployment 到 version 28，正式 `/exec` URL 不變。
2. Firebase Hosting 已部署學員端與講師端。
3. 線上學員端與講師端回應 `200`，皆載入 `app.js?v=0.3.12`。
4. 線上學員端 HTML 已包含放棄創作回答按鈕。
5. 線上講師端 HTML 已包含電腦學員控制按鈕。
6. GAS `getGameState` 回應 `ok:true`。
7. GAS `addComputerPlayers` 與 `submitComputerAnswers` 已不再回覆「未知 action」；未帶管理密碼時會正確回覆「管理操作授權失敗」。
8. 本次未部署 Cloud Functions、Firestore rules 或 Realtime Database rules。

## 0.3.13 修正範圍

Status：`0.3.13` 已完成並部署雲端。

Root Cause：`0.3.12` 已補上創作題限時與電腦學員，但學員端仍有大量自動 GAS 呼叫，200 人同時操作時會造成 Apps Script 延遲；創作題倒數有多個 timer 同時更新；創作決選結果尚未套用戰隊加分；競賽結束後缺少正式結算與學員端最後成績顯示。

Suggested Fix：降低自動讀取頻率，改由個人摘要同步紅點；修正創作倒數 timer；新增競賽結算 API，結算時套用創作票選加分與獎項，並讓學員端顯示最後結果。

完成項目：

1. 學員端登入後只自動讀取個人摘要，不再自動讀取寶箱、成就與匿名決選。
2. 關題關閉後只自動更新個人摘要，不再自動讀取排行榜、寶箱、成就與匿名決選。
3. `getPlayerSummary` 回傳寶箱紅點與成就紅點狀態。
4. 創作題倒數停止與一般題倒數共用同一顯示來源，避免閃爍。
5. 戰隊積分顯示無條件進位到個位數。
6. 學員端避免用暫時性 0 分覆蓋既有戰隊積分。
7. 結算競賽時，創作決選第一名戰隊取得預設 20 分加分。
8. GAS 新增 `finalizeCompetition` 與 `getFinalResults`。
9. 講師端新增「結算競賽」按鈕。
10. 學員端新增最後成績區與上台領獎提示。

測試與部署：

1. GAS 語法檢查、前端 JavaScript 語法檢查、JSON 解析、`git diff --check`、`npm run check:functions` 通過。
2. 本機學員端與講師端靜態頁面回應 `200`，皆載入 `app.js?v=0.3.13`。
3. GAS 已推送並更新既有 Web App deployment 到 version 30，正式 `/exec` URL 不變。
4. Firebase Hosting 已部署學員端與講師端。
5. 線上學員端與講師端回應 `200`，皆載入 `app.js?v=0.3.13`。
6. GAS `getGameState` 回應 `ok:true`。
7. GAS `finalizeCompetition` 與 `getFinalResults` 已不再回覆「未知 action」。
8. 本次未部署 Cloud Functions、Firestore rules 或 Realtime Database rules。

## 0.3.14 免費方案效能重構第一階段

Status：`0.3.14` 已完成並部署雲端。

Root Cause：`0.3.13` 雖已降低部分自動 GAS 呼叫，但送答、道具使用、開箱、成就領取與排行榜仍會依賴 GAS / Google Sheets 或即時全量刷新；200 人活動時仍會在高峰操作造成延遲。

Suggested Fix：先把最高頻且可延後結算的學員操作改為 Firebase 快速寫入與快照讀取，正式成績仍由賽後 GAS 重新計分。

完成項目：

1. 學員送答優先寫入 `answers/{gameId}/{questionId}/{playerId}`。
2. 送答後立即停用選項並顯示「已送出，等待講師關題」。
3. 已從 Firebase 公開題庫取得題目時，不再呼叫 GAS `openPaper`。
4. 道具使用優先寫入 `itemUses/{gameId}/{itemId}`，狀態為 `pending`。
5. 道具使用後不刷新排行榜、寶箱、成就與個人摘要。
6. 排行榜優先讀取 `publicScoreboards/{gameId}` 快照；無快照時保留 GAS 備援。
7. 成就領取改為寫入 `achievementClaimRequests`。
8. 寶箱開啟改為寫入 `treasureBoxOpenRequests`，並立即隱藏該列。
9. Realtime Database rules 新增高頻互動資料節點限制。

尚未完成項目：

1. Firebase `players` 報到流程。
2. Firebase 創作投稿與投票流程。
3. 講師端關題後批次建立暫時排行榜快照。
4. GAS 從 Firebase 匯出並正式重新計分。
5. 寶箱取得時預先決定 `rewardType`。
6. Firebase Auth 身分驗證與更嚴格 rules。

測試與部署：

1. GAS 語法、前端 JavaScript 語法、JSON 解析、`git diff --check`、`npm run check:functions` 通過。
2. 本機學員端與講師端靜態頁面回應 `200`，皆載入 `app.js?v=0.3.14`。
3. Realtime Database rules 已通過 Firebase CLI dry run。
4. 已部署 Firebase Hosting 學員端與講師端。
5. 已部署 Realtime Database rules。
6. 線上學員端與講師端回應 `200`，皆載入 `app.js?v=0.3.14`。
7. 線上 Realtime Database 測試：第一次送答寫入成功，第二次覆寫被拒絕；測試資料已移除。
8. 本次未部署 GAS、Cloud Functions、Firestore rules、Cloud Run 或任何需付費帳務的服務。

## 第 3 版不做事項

1. 不升級 Firebase Blaze，除非使用者明確改變免費方案限制。
2. 不把抽獎或道具判定放在前端。
3. 不把正確答案、管理密碼、Token 或服務帳戶金鑰放入 Git。
4. 不在 Realtime Database 保存完整作答紀錄、完整投稿審核紀錄或管理操作密碼。
5. 不一次實作所有第 3 版功能；每個子版本需可獨立測試與還原。

## 第 3 版測試要求

每個第 3 版子版本至少需完成：

1. JSON 設定檔解析檢查。
2. GAS 語法檢查。
3. 前端 JavaScript 語法檢查。
4. `npm run check:functions`。
5. 第 2 版核心流程回歸測試：
   - 報到。
   - 開題。
   - 翻卷。
   - 作答。
   - 關題計分。
   - 排行榜。
6. 本機測試通過後，先回報使用者，不直接部署雲端。
## 2026-05-23：0.3.16 免費方案效能重構續作

本次完成範圍：

1. 創作題投稿優先寫入 Firebase Realtime Database `creativeSubmissions/{gameId}/{questionId}/{playerId}`。
2. 隊內初選投票優先寫入 `creativeTeamVotes/{gameId}/{questionId}/{playerId}`。
3. 匿名全體投票優先寫入 `creativeFinalVotes/{gameId}/{questionId}/{playerId}`。
4. 投稿與投票成功後立即回饋，不再立即重讀投稿池或決選作品。
5. Realtime Database rules 新增創作投稿與投票節點限制，同一路徑不可重複覆寫。

後續仍需處理：

1. 講師端關題後建立 `publicScoreboards` 暫時排行榜快照。
2. GAS 從 Firebase 匯出比賽資料並正式重新計分。
3. 寶箱取得時預先決定 `rewardType`。
4. Firebase Auth 身分驗證與更完整的 rules。

## 2026-05-23：0.3.15 免費方案效能重構續作

本次完成範圍：

1. 學員報到改為優先寫入 Firebase Realtime Database `players/{gameId}/{playerId}`。
2. 報到成功後立即進入遊戲畫面，不等待 GAS、個人摘要、排行榜、寶箱或成就資料。
3. 未開放自由選隊時，前端以 `clientKey` 雜湊穩定分配戰隊。
4. 保留 GAS `joinGame` 備援，避免 Firebase rules 或網路異常時現場無法報到。
5. Realtime Database rules 新增 `players` 寫入限制，同一路徑不可重複覆寫。

後續仍需處理：

1. 同暱稱跨裝置去重。
2. 創作投稿、隊內投票、匿名全體投票 Firebase 化。
3. 講師端關題後建立 `publicScoreboards` 暫時排行榜快照。
4. GAS 從 Firebase 匯出比賽資料並正式重新計分。
5. 寶箱取得時預先決定 `rewardType`。
6. Firebase Auth 身分驗證與更完整的 rules。
